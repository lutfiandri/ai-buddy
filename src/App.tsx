import { Button } from '@/components/ui/button';
import { Layout } from '@/components/layout/layout';

function App() {
  return (
    <Layout>
      ingfo <Button>p</Button>
    </Layout>
  );
}

export default App;
